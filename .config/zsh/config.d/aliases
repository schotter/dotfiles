alias sshts="ssh ssh-w0145b40@w0145b40.kasserver.com"
alias sshlcb="ssh ssh-w016eaa8@w016eaa8.kasserver.com"
#alias mpv="mpv --no-audio-display"
#alias usbview="sudo usbview"
alias mpv="mpv --no-audio-display"
#alias sshreceiver="ssh root@axodin.fritz.box"
#alias vi=lvim
#alias vim=lvim
alias firefox="firefox -private-window"
#alias dd="dd status=progress"
#alias freecad="QT_QPA_PLATFORM=xcb freecad"
#alias xterm="xterm -fa Monospace -fs 18 -bg black -fg grey"
alias youtube-dl="yt-dlp"
alias vi="nvim"
alias cfg='git --git-dir="$HOME/.cfg.git" --work-tree="$HOME"'
#cfg config --local status.showUntrackedFiles no

#fzf-dmenu() {
#	# note: xdg-open has a bug with .desktop files, so we cant use that shit
#	selected="$(ls /usr/share/applications | fzf -e)"
#	nohup `grep '^Exec' "/usr/share/applications/$selected" | tail -1 | sed 's/^Exec=//' | sed 's/%.//'` >/dev/null 2>&1&
#}
